import {S,log} from './state.js';import {rad} from './utils.js';
export function generateGrid(rows,cols,spacingD){const {lat,lon,rotorD,hubHeight}=S.project;const spacing=spacingD*rotorD;const mx=111320*Math.cos(rad(lat)),my=111320;const out=[];let id=1;for(let r=0;r<rows;r++)for(let c=0;c<cols;c++){const x=(c-(cols-1)/2)*spacing,y=(r-(rows-1)/2)*spacing;out.push({id:id++,name:`T${id-1}`,lat:lat+y/my,lon:lon+x/mx,hh:hubHeight})}S.turbines=out;log(`Generated ${out.length} turbines (${spacingD}D spacing)`);return out}
export function parseLayout(text){let pts=[];if(text.includes('<kml')||text.includes('<coordinates>')){const re=/<coordinates>([\s\S]*?)<\/coordinates>/g;let m;while((m=re.exec(text))){const toks=m[1].trim().split(/\s+/);for(const t of toks){const [lon,lat]=t.split(',').map(Number);if(Number.isFinite(lat)&&Number.isFinite(lon))pts.push({lat,lon})}}}else{text.split(/\r?\n/).forEach((line,i)=>{if(!line.trim())return;const c=line.split(/[;,\t]/).map(x=>x.trim());const nums=c.map(Number);let lat,lon,name=`T${i+1}`;if(Number.isFinite(nums[1])&&Number.isFinite(nums[2])){name=c[0]||name;lat=nums[1];lon=nums[2]}else if(Number.isFinite(nums[0])&&Number.isFinite(nums[1])){lat=nums[0];lon=nums[1]}if(Number.isFinite(lat)&&Number.isFinite(lon))pts.push({name,lat,lon})})}const seen=new Set();pts=pts.filter(p=>{const k=p.lat.toFixed(7)+','+p.lon.toFixed(7);if(seen.has(k))return false;seen.add(k);return true}).map((p,i)=>({id:i+1,name:p.name||`T${i+1}`,lat:p.lat,lon:p.lon,hh:S.project.hubHeight}));S.turbines=pts;log(`Loaded ${pts.length} unique turbine points`);return pts}
